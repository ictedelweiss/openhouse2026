# Git Configuration Rules

- **GitHub Repository**: `https://github.com/ictedelweiss/openhouse2026.git`
- **Git Push Command**: Always push using `git push origin main` (where origin URL is configured in git remote settings).
